import Alert from "react-bootstrap/Alert";
import Button from "react-bootstrap/Button";

function ToastAlert(show, itemName) {
	return (
		<>
			{/* <Alert show={show} variant="success">
				{itemName} added!
			</Alert> */}
		</>
	);
}

export default ToastAlert;
